package app

var (
	output, outputParams, separators, pattern, indexFile string
	maxChildren, scanCount, throttleNs, workers          int
	logLevel                                             string
)
